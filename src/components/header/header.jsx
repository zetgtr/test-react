/**
 * Функция создания header
 * @returns Возвращает header
 */
const Header = () => {
  return (
    <header>
      <div className="header conteiner ">
        <p>Тестовое задание на React</p>
      </div>
    </header>
  );
};

export default Header;
